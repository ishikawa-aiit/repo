declare module "../MemoryRouter" {
    interface MemoryRouter {
        /**
         * Manually registers the paths in your "pages" folder, including slugs,
         * so that static and dynamic routes are parsed correctly.
         *
         * @example
         * import mockRouter from "next-router-mock";
         * import "next-router-mock/dynamic-routes";
         *
         * mockRouter.registerPaths([
         *   // These paths should match those found in the `/pages` folder:
         *   "/[id]",
         *   "/static/path",
         *   "/[dynamic]/path",
         *   "/[...catchAll]/path"
         * ]);
         *
         */
        registerPaths(paths: string[]): void;
    }
}
declare type AbstractedNextDependencies = Pick<typeof import("next/dist/shared/lib/router/utils"), "getSortedRoutes" | "getRouteMatcher" | "getRouteRegex" | "isDynamicRoute"> & Pick<typeof import("next/dist/server/normalize-page-path"), "normalizePagePath">;
export declare function defineRegisterPaths({ getSortedRoutes, getRouteMatcher, getRouteRegex, isDynamicRoute, normalizePagePath, }: AbstractedNextDependencies): void;
export {};
